import Foundation

/// Placeholder for **SpeechAnalyzer** streaming pipeline (PRD §7). Wire when iOS SDK exposes public API; `SpeechRecognizerService` is the supported fallback today.
enum SpeechAnalyzerPlaceholder {
    static var isEnabled: Bool { false }
}
